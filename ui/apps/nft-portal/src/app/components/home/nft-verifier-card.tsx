import { PageCard } from './page-card';

export function NFTVerifierCard() {
  return <PageCard title='NFT Metadata Verifier' description='Verify NFTs meet all recommended metadata standards and have your NFT added to the NFT Catalog' href='/v' />;
}